# Copy this file to .env and fill in real values. Never commit .env.

# Local SQLite database (only needed if/when a database is added)
DATABASE_URL=file:./db/custom.db

# Public site URL used for SEO metadata (src/app/layout.tsx)
NEXT_PUBLIC_SITE_URL=https://safeglobal.com
